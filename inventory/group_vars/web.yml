---
base_hardening_firewall_allowed_tcp: [22, 80, 443]

nginx_hsts_enabled: false
nginx_sites: []

patch_orchestration_required_services: [nginx]
patch_orchestration_health_url: "http://{{ ansible_host }}/healthz"
